# Inputs
This script aims at scanning a web site and determing the pages where there are some `user input fields`basing on a URL. 
This given URL will serve as `root` of the scan. Any `subpage` is considered in the scope.
